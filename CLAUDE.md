<!-- AUTO-GENERATED by lib/write_claude_md.py — DO NOT EDIT MANUALLY -->
<!-- Use `/dev-kit:bootstrap --refresh-claude-md` or `--slim|--full` to regenerate. -->

# CLAUDE.md — dev-harness-kit codebase map + iron laws

> Last generated: 2026-07-04T15:30:00 KST by `/dev-kit:bootstrap`
> Mode: `--slim-claude-md` (default). Use `:full` opt-in for complete codebase map.
> Manual edit blocked by hook (use `--strict` or explicit refresh).

---

## §1 Iron Laws (SSOT — single source, MUST-8)

- **L1 No-Test-No-Code**: prod code without verification artifact (test / contract / domain test / scenario / feature flag per methodology) ❌.
- **L2 Root-Cause-First**: fix without reproducing the bug ❌. Phase 1 = reproduce.
- **L3 Evidence-Before-Done**: completion claim with quoted exit code / test count / build log only. "should work" / "probably fine" ❌.
- **L4 No-Stub**: TODO / FIXME / "we'll extend later" / "this is a starting point" ❌.
- **L5 Lean-Output**: option/alternative list when not asked ❌. One answer.

(hooks emit "Iron Law #N violation" stderr only. Bodies not duplicated.)

## §2 Active Stage

- current_stage: Bootstrap
- current_step: 1/1
- hand_off_chain: []
- shortcut_used: none
- methodology: tdd (MUST-48 default)

## §3 Codebase Map (5-line STUB, opt-in `:full`)

```
project_root: /Users/sanghee/dev/dev-harness-kit (Bootstrap skeleton)
tree:
  - .claude-plugin/{marketplace,plugin/{plugin,hooks}}.json
  - skills/<category>/<skill-name>/SKILL.md (13 skills, 9 categories)
  - commands/<command>.md (15 commands, 0-arg)
  - lib/{state_codec,active_hooks_codec,write_claude_md,eval_runner,llm_judge,
        sub_agent_runner,a2a_codec,fixers/*,install.sh}.py
  - eval/{golden,prompts,fixtures}/ (golden 13 + judge prompts 3)
  - docs/{STAGES,ARCHITECTURE,NAMING,COST-ANALYSIS,PRIVATE-REPO-SETUP,
         INTEGRATION,AX,PRE-IMPL-CHECK,METHODOLOGY}.md
```

<!-- Run `/dev-kit:bootstrap --full-claude-md` to embed complete tree (depth 4), manifest, deps -->

manifest:
  - python >= 3.10
  - bash >= 3.2 (macOS compatible)
  - jq (for hooks JSON parsing)

conventions:
  - directory names: `skills/<category>/<skill-name>/` (kebab-case, ADR-0010)
  - skill frontmatter: `name` MUST match directory; `category` MUST ∈ 9
  - slash commands: `/dev-kit:<stage>` (0-arg, MUST-21)
  - state files: `.dev-kit/{state.json, .active-hooks.json, .enabled.json}`

## §4 Hook Matrix (`active-hooks.json` stage-aware, MUST-13)

| Hook           | Boot | Plan | Design | Build | Review | Security | Ship |
|----------------|:----:|:----:|:------:|:-----:|:------:|:--------:|:----:|
| tdd-guard      |  -   |  -   |   -    |  ✅   |   -    |    -     |  -   |
| bash-guard     |  -   |  -   |   -    |  ✅   |   -    |    -     |  -   |
| secret-scan    |  R   |  -   |   -    |  ✅   |   ✅   |    ✅    |  -   |
| slop-detector  |  -   |  -   |   -    |  ✅   |   ✅   |    ✅    |  -   |
| stop-verify    |  -   |  ✅  |   ✅   |  ✅   |   ✅   |    ✅    |  ✅  |

(R = read-only)

## §5 Hand-off Pointer

```
.previous_hand_offs: []
.pending_hand_offs: []
.next_stage_trigger: /dev-kit:plan
.shorcut_trigger: /dev-kit:tdd-fast
```

## §6 Eval Status (MUST-32~34, will be filled after /dev-kit:eval)

```
eval_4_axis_scores: pending
golden_baseline: eval/golden/*.json (to be created in Phase 4)
last_eval_run: null
drift_warnings: 0
rot_assets: 0
```

## §7 Methodology (MUST-48, currently TDD)

```
active_methodology: tdd
available: tdd | sdd | ddd | bdd | fdd | <custom>
selection: /dev-kit:config → "methodology" question
adapter_path: lib/methodology/<name>.py
```

## §8 User Role Boundary (MUST-35, MUST-54~55)

```
user_does:
  - Planning (1-line goal + AC 1~5 + non-goals)
  - Spec (PRD.md, 1 page)
  - Decisions (gate decisions + cost review + stage approvals)
  - Review (PR-level + Eval-Repair Human Review)

user_does_NOT:
  - Decompose stages (system = decompose)
  - Implementation (system = execute)
  - Write cost-question format (AI = present)
  - Manage pre-commit hook + CI (auto)
```

## §9 Iron Laws Validation (eval targets)

`/dev-kit:eval` judge this file's 4 axes:
- semantic_drift: vs eval/golden/claude-md.md baseline
- completeness: all 9 sections present + §1 ≤200 lines
- correctness: §4 hook matrix matches `.active-hooks.json`
- consistency: §1 iron laws match `lib/write_claude_md.py:L1-L5_DEFINITIONS`

(eval not yet run — Phase 3 will populate `eval/golden/claude-md.md`.)

<!-- END AUTO-GENERATED -->
