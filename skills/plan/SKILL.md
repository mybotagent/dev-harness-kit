---
name: plan
category: plan
description: 0-arg plan stage. Run a `/dev-kit:plan` session — dispatches to the `plan-ralph` SKILL.
disable-model-invocation: true
---

Run a `/dev-kit:plan` session, using the `plan-ralph` skill.
